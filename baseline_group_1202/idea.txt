1. 调整参数

2. 换更大的模型 0.5, Qwen2.5-7B，7200*2.

3. 训练阶段可以做的：
3.1. 加入参考文档在prompt里
3.2. 太长的参考文档，response可以让模型先生成一个简短的
# 3.3. 5个对话可以生成4个训练集，以此来扩充数据集

4. 推理阶段可以做的：
4.1. 调整回答的长度
4.2. 检索相关的文档加入到prompt (RAG)


5. 后处理(推理阶段)
4.3. 后处理，相似度向量判断后挨个判断


5. 看数据集和论文表示



看数据
(看数据集和论文表示) -> (分测试集和训练集) -> 调整prompt模版 -> 加入参考文档在prompt里(先让模型生成简短的) -> 检索相关的文档加入到prompt -> 后处理 -> 增大训练集，换更大的模型肯定可以增加性能 -> 调整参数


1. 判断下在验证集上的规律和测试集上的规律是否是一致的
2. 推理和训练可以并行，训练优化要先增大数据集才可以


全餐微调 